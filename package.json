{
  "name": "mcp-oss-onramp",
  "version": "0.1.0",
  "description": "MCP server to scout beginner-friendly OSS issues",
  "type": "module",
  "bin": {
    "mcp-oss-onramp": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc -w",
    "start": "node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}
